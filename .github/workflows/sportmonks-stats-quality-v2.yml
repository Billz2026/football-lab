name: Sportmonks player stats quality v2

on:
  push:
    branches: [main]
    paths:
      - 'scripts/analyse-sportmonks-player-stats.mjs'
      - '.github/workflows/sportmonks-stats-quality-v2.yml'
      - 'data/provider/sportmonks-stats-quality.trigger'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sportmonks-player-stats-quality-v2
  cancel-in-progress: true

jobs:
  analyse:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Verify secret is configured
        shell: bash
        env:
          SPORTMONKS_API_TOKEN: ${{ secrets.SPORTMONKS_API_TOKEN }}
        run: |
          set -euo pipefail
          test -n "${SPORTMONKS_API_TOKEN:-}"

      - name: Rebuild ephemeral Scottish squad staging
        env:
          SPORTMONKS_API_TOKEN: ${{ secrets.SPORTMONKS_API_TOKEN }}
        run: node scripts/import-sportmonks-scotland.mjs data/staging/private/sportmonks-scottish-premiership-2026-27.json artifacts/squad-quality-temp.json

      - name: Analyse player statistics and provisional ratings
        env:
          SPORTMONKS_API_TOKEN: ${{ secrets.SPORTMONKS_API_TOKEN }}
          SPORTMONKS_STATS_CONCURRENCY: '8'
        run: node scripts/analyse-sportmonks-player-stats.mjs data/staging/private/sportmonks-scottish-premiership-2026-27.json artifacts/sportmonks-player-stats-quality-report.json

      - name: Verify no raw API data is published
        shell: bash
        run: |
          set -euo pipefail
          test -f artifacts/sportmonks-player-stats-quality-report.json
          test -f data/staging/private/sportmonks-scottish-premiership-2026-27.json
          rm -f artifacts/squad-quality-temp.json
          rm -rf data/staging/private

      - name: Upload sanitised stats quality report only
        uses: actions/upload-artifact@v4
        with:
          name: sportmonks-player-stats-quality-${{ github.run_id }}
          path: artifacts/sportmonks-player-stats-quality-report.json
          if-no-files-found: error
          retention-days: 3
