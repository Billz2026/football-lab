# Football Lab (Web)

A lightweight HTML/CSS/JS project hosted on GitHub Pages.

## Structure
- Home: `/index.html`
- First mode: `labs/set-pieces/`

## Run locally
Open `index.html` in your browser (or use a simple local server).

## Deploy (GitHub Pages)
Repo Settings → Pages → Deploy from branch → main → /(root)

