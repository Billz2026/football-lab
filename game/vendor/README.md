# Football Lab vendor assets

These files are the browser ESM distribution of `three@0.180.0` and its GLTF loader dependencies.

They are kept local so the character renderer does not depend on a runtime CDN request. The source package is licensed under MIT; see `THREE-LICENSE.txt`.
